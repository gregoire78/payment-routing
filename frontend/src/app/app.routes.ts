import { Routes } from '@angular/router';
import { MessagesScreen } from './features/messages-screen/messages-screen';

export const routes: Routes = [
	{
		path: '',
		component: MessagesScreen
	}
];
